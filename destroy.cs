using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class destroy : MonoBehaviour {



	void OnCollisionEnter2D(Collision2D col){
	  
		if (col.gameObject.name == "caja1") {

			Destroy (col.gameObject);

		}
		if (col.gameObject.name == "caja2") {

			Destroy (col.gameObject);

		}
		if (col.gameObject.name == "caja3") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja4") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja5") {

			Destroy (col.gameObject);

		}
		if (col.gameObject.name == "caja6") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja7") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja8") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja9") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja10") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja11") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja12") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja13") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja14") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja15") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja16") {

			Destroy (col.gameObject);

		}

		if (col.gameObject.name == "caja17") {

			Destroy (col.gameObject);

		}
		}
	
	}
	

